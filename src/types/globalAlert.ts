export type GlobalAppAlert = {
  kicker: string
  title: string
  message: string
  is_active: boolean
  updated_at: string
}

export type GlobalAppAlertInput = {
  kicker: string
  title: string
  message: string
  isActive: boolean
}
