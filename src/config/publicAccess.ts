/**
 * Demo sin login — SOLO desarrollo local explícito.
 * Producción (Vercel): NO definir VITE_PUBLIC_DEMO o dejarlo en "false".
 */
export const PUBLIC_DEMO_ACCESS = import.meta.env.VITE_PUBLIC_DEMO === 'true'
